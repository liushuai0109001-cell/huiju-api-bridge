# 荟聚 API 外接软件：洛水三个创作模式配置使用说明

适用版本：洛水 v2.3.4、荟聚 API 外接软件 Go 桌面版。

本文说明如何让“漫剧解说、剧本创作、自由/超级画布”统一使用自己的中转站，不使用洛水内置中转站。音色和配音不在本文接管范围内。

## 一、配置原则

洛水中选择的模型是“入口模型”，作用是让洛水按固定协议发出请求。真正提交给第三方的模型由荟聚 API 外接软件配置：

| 请求类型 | 洛水本地入口 | 外接软件接收接口 | 中转站接口 |
|---|---|---|---|
| 语言 | 洛水后缀语言模型 | `POST /v1/chat/completions` | `POST /v1/chat/completions` |
| 图片 | 洛水出图包 / Nano2 出图包 | `POST /v1/images` 或 `/v1/images/generations` | `POST /v1/images/generations` |
| 视频 | 本地 Grok / Grok 视频包 1.0 | `POST /v1/videos` | `POST /v1/videos` |
| 视频状态 | 洛水自动轮询 | `GET /v1/videos/{id}` | 同路径 |
| 视频文件 | 洛水自动下载 | `GET /v1/videos/{id}/content` | 同路径 |

外接软件会覆盖请求中的 `model`。因此洛水显示 `grok-imagine-1.0-video`，中转站实际使用 `sd2.5` 是正常现象。

## 二、先配置荟聚 API 外接软件

1. 打开 `荟聚API外接软件.exe`。
2. 在“语言模型”页填写自己的中转站 Base URL、真实 API Key 和目标语言模型。
3. 在“图片模型”页填写同一中转站或另一图片站点的 Base URL、真实 API Key 和目标图片模型。
4. 在“视频模型”页填写支持 OpenAI 兼容 `/v1/videos` 的 Base URL、真实 API Key 和目标视频模型。
5. 点击“保存配置”。
6. 确认本地服务状态正常，然后点击外接软件中的“启动洛水”。

Base URL 可以填写站点根地址，例如：

```text
https://your-relay.example.com
```

也可以填写带 `/v1` 的地址：

```text
https://your-relay.example.com/v1
```

真实 API Key 只保存在外接软件中，不要把真实中转站 Key 填进洛水。

## 三、洛水全局设置

在洛水“全局设置”中填写以下入口配置。这里的 Key 只用于通过洛水的非空校验，可以统一使用 `local-proxy`。

| 洛水设置项 | 建议值 | 用途 |
|---|---|---|
| 洛水中转通用 API Key | `local-proxy` | 语言模型入口 |
| 洛水大香蕉 / 洛水出图包 API | `local-proxy` | 图片模型入口 |
| 本地 Grok 视频服务地址 | `http://localhost:5400` | 本地视频入口 |
| 本地 Grok 视频服务 API Key | `local-proxy` | 本地视频非空校验 |
| 洛水渠道 Grok 视频包 API | `local-proxy` | 画布和部分剧本视频入口非空校验 |
| 本地 Grok 图片服务地址 | `http://localhost:8000` | 旧版/本地图片兼容入口 |
| 本地 Grok 图片服务 API Key | `local-proxy` | 图片兼容入口非空校验 |

如需使用参考图生成视频，洛水的 Grok 视频包会先调用独立图床上传参考图。此时还要配置“图床接口”。不配置图床时请使用纯文本视频；这不会影响语言、文生图和纯文本视频。

当前外接软件已经增加“图床上传”页。洛水中的“图床接口”只填写 `local-proxy`，真实图床上传 URL 和 Key 填在外接软件中。外接软件会把洛水的参考图上传请求转到图床，并通过 `X-Upload-Key` 发送真实 Key。

每次必须从荟聚 API 外接软件点击“启动洛水”，或者使用外接软件提供的启动方式。直接双击原始 `洛水.exe` 可能不会带上外接代理环境。

## 四、时长和画面比例在哪里设置

外接软件现在提供可直接选择的比例和时长选项，不需要填写 JSON。

- 图片比例：在洛水的图片节点、分镜生图或项目生图设置中选择。
- 视频比例：在洛水的视频节点或视频生成面板中选择。
- 视频时长：洛水界面提供目标时长时，直接在洛水选择。
- 外接软件选择“跟随洛水”时，保留洛水中的图片或视频比例。

图片比例会被外接软件转换为上游常见的 `size`。例如：

| 洛水比例 | 默认上游图片尺寸 |
|---|---|
| `1:1` | `1024x1024` |
| `9:16`、`3:4` | `1024x1536` |
| `16:9`、`4:3` | `1536x1024` |

图片模型页可直接选择 `1:1`、`16:9`、`9:16`、`4:3`、`3:4`，外接软件会转换为上游常用图片尺寸。

视频模型页的时长规则：

| 外接软件选项 | 最终规则 |
|---|---|
| `15 秒（跟随洛水设置）` | 不覆盖时长，最终采用洛水请求中的值 |
| `30 秒（外置软件优先）` | 无论洛水请求 15 秒或其他时长，最终都向中转站发送 30 秒 |

外接软件会自动兼容上游的 `seconds` 和 `duration` 字段。视频比例也直接从下拉选项选择；选择“跟随洛水”时不覆盖。

## 五、漫剧解说模式

### 语言模型

在项目的推理/语言模型中选择带“洛水”后缀的 OpenAI 兼容模型。已验证入口：

```text
gemini-3.1-pro-preview-洛水
```

洛水中的模型名仅负责进入 `/v1/chat/completions`，实际模型以外接软件“语言模型”页为准。

### 图片模型

在生图来源中选择：

```text
洛水渠道 / nano2-出图包
```

界面具体模型可选择 `gpt-image2-2k-出图包` 等洛水出图包模型。实际图片模型以外接软件“图片模型”页为准。

### 视频模型

优先选择：

```text
本地Grok
grok-imagine-1.0-video
```

推荐先使用 10 秒、720P。提交后洛水会自动创建任务、轮询状态、获取 `/content` 并把 MP4 放回项目视频目录。

## 六、剧本创作模式

### 语言模型

剧本推理、分镜推理和提示词推理选择：

```text
gemini-3.1-pro-preview-洛水
```

也可以选择其他带“洛水”后缀、最终走 OpenAI Chat Completions 协议的模型。

### 图片模型

在剧本引擎的“生图”和“改图”中统一选择：

```text
洛水大香蕉2生图包 / nano2-出图包
```

对应内部适配器通常为 `luoshui_nano_banana`。生图和改图可以共用外接软件的图片目标模型。

### 视频模型

在剧本视频引擎中选择以下任一已接管入口：

```text
本地Grok / grok-imagine-1.0-video
Grok视频包-1.0 / grok-imagine-1.0-video
```

若界面同时存在两项，优先使用“本地Grok”；只有“Grok视频包-1.0”时也可使用，运行时会定向到 `localhost:5400/v1/videos`。

## 七、自由/超级画布模式

### 语言模型

画布聊天和提示词推理选择带“洛水”后缀的语言模型，规则与剧本创作相同。

### 图片节点

在图片节点的模型选择器中选择：

```text
洛水大香蕉2生图包
```

其内部值为 `luoshui_nano_banana`，请求由本地图片接口转发到外接软件配置的真实图片模型。

### 视频节点

1. 新建或选中视频节点。
2. 点击“生成”。
3. 在 Grok 视频包分组选择 `grok-imagine-1.0-video`。
4. 推荐参数：`16:9`、`720P`、`10s`、生成数量 `1`。
5. 不使用参考图时直接填写提示词并发送。

画布中该入口显示为：

```text
Grok视频包-1.0
grok-imagine-1.0-video
```

已实测请求会经过 Go 外接软件，目标模型可被改写为外接软件设置的 `sd2.5`，生成完成后 `videoPath` 会自动写回画布节点。

## 八、不要选择的模型入口

以下引擎通常使用各自专有 API、网页账号、CLI 或 ComfyUI 协议，不经过当前 OpenAI 兼容外接软件：

- 火山 Seedance 官方 API
- 即梦浏览器版、Vidu 浏览器版等网页登录引擎
- 即梦 CLI、Flova CLI、LibTV CLI 等本机 CLI
- ComfyUI 工作流
- RunningHub 专有工作流接口
- 其他明确标注官方 API 或专有渠道的模型

要验证外接软件时，只选择本文列出的“洛水后缀语言模型、洛水出图包、本地 Grok 或 Grok 视频包 1.0”。

## 九、判断是否接管成功

打开外接软件日志，正常请求应出现以下记录：

```text
CHAT POST /v1/chat/completions
IMAGE POST /v1/images
VIDEO POST /v1/videos
VIDEO GET /v1/videos/{task_id}
VIDEO GET /v1/videos/{task_id}/content
UPLOAD POST /upload
```

多参考图视频提交还会出现：

```text
VIDEO submit request=video-000001 ... references=2 fields=images,reference_images
VIDEO submitted request=video-000001 upstream_task=task_xxx references=2
```

两行的 `request` 必须相同。第二行的 `upstream_task` 是需要在中转站核查的最终任务号，`references` 是该任务实际携带的去重后参考图数量。上传日志中的图片 URL 不能单独证明某个视频任务已经参图。

状态轮询示例：

```text
UPLOAD 成功：参考图片已上传图床
VIDEO 提交成功：...，洛水开始轮询视频状态
VIDEO 状态查询：task=task_xxx stage=正在生成视频 status=processing progress=42%
VIDEO 成功：视频文件已生成，正在把内容返回给洛水
```

视频最终应同时满足：

1. 上游状态为 `completed`，进度为 `100`。
2. 日志出现 `/content` 请求。
3. 洛水项目目录产生 MP4 文件。
4. 洛水分镜或画布节点出现可播放视频。

如果洛水提示任务已提交但日志没有新请求，通常是选错了模型入口，或洛水在发出 HTTP 请求前因某个必填 Key、参考图图床配置而终止。

## 十、当前实测状态

- 漫剧解说：视频创建、轮询、下载并回填成功。
- 剧本创作：语言、图片和视频共用桥接协议，链路已验证。
- 自由/超级画布：纯文本视频生成并回填视频节点成功。
- 图床上传：洛水参考图上传适配器经本机 `/upload` 转发并返回公网 URL 成功。
- 音色/配音：按要求跳过，外接软件当前不接管该协议。
