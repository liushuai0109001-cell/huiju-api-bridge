# 洛水 v2.3.4 三模式链路分析

分析日期：2026-08-16。

## 结论

“漫剧解说”“剧本创作”“超级画布（当前界面标题为自由画布）”不是三套独立生成 API。三者在业务层组织不同，但最终复用语言、图片、视频三类生成适配器。因此桥接层应按请求类型路由，不应按界面名称路由。

## 运行确认

### 漫剧解说

- 项目引擎设置的生图来源：`洛水渠道`。
- 固定生图入口：`nano2-出图包`。
- 项目中显示的实际模型名：`gpt-image2-2k-出图包渠道2`。
- 固定视频入口：`本地Grok`。
- 项目中显示的视频模型名：`grok-imagine-1.0-video`。
- 视频参数包含时长、尺寸、质量和是否仅发送分镜图。

### 剧本创作

- 项目语言模型：`gemini-3.1-pro-preview-洛水`。
- 项目配置文件中的生图/改图适配器：`luoshui_nano_banana`。
- 界面显示的生图/改图入口：`nano2-出图包`。
- 分镜阶段会调用语言模型，已有运行记录显示请求经过 OpenAI 兼容客户端。
- 项目已有真实图片和视频结果，说明剧本模式最终复用同一图片/视频适配层。

### 超级画布 / 自由画布

- 画布可导入剧本角色、场景、道具和分镜节点。
- 导入节点的 `genModel` 保存为 `luoshui_nano_banana`。
- 画布没有独立的第四类生成协议，仍复用图片/视频适配层。

## 可接管接口

| 类型 | 洛水侧接口 | 桥接行为 |
|---|---|---|
| 语言 | `POST /v1/chat/completions` | 保留 messages 等任务字段，替换 model，注入配置的覆盖参数 |
| 图片 | `POST /v1/images` | 兼容改写为 `/v1/images/generations`，替换 model |
| 图片 | `POST /v1/images/generations` | 原路径转发，替换 model |
| 视频 | `POST /v1/videos` | 创建任务，替换 model，保留 prompt、图片和视频参数 |
| 视频 | `GET /v1/videos/{id}` | 原样轮询上游任务 |
| 视频 | `GET /v1/videos/{id}/content` | 原样获取生成媒体 |
| 模型 | `GET /v1/models` | 用于管理页拉取中转站模型列表 |

洛水根目录的 `sitecustomize.py` 在设置 `LUOSHUI_USE_EXTERNAL_PROXY=1` 后，会把使用 `api.aibh.site` 的 OpenAI 客户端重定向到 `http://localhost:5400/v1`。洛水的 `本地Grok` 同样使用本地 `5400` 链路。`8000` 作为旧版/本地生图兼容端口保留。

## 不接管范围

- 洛水账号、授权、更新和官方认证接口。
- 浏览器账号型引擎（例如需要网页登录态的第三方平台）。
- ComfyUI 专有的 `/prompt`、`/history` 工作流协议；当前第一版仅实现 OpenAI 兼容中转站。

## 字段处理原则

1. 只覆盖 `model` 和用户在管理页明确配置的参数。
2. `prompt`、`messages`、参考图、尺寸、时长等洛水任务字段默认保留。
3. API Key 只放在发往上游的 `Authorization: Bearer` 请求头中。
4. 默认不记录请求正文，避免剧本、提示词和参考图数据进入日志。
5. 上游 Base URL 同时支持填写站点根地址或以 `/v1` 结尾的地址。

## 当前限制

不同中转站的视频返回 JSON 可能采用不同字段名。当前版本按 OpenAI 兼容视频接口透传；若中转站使用自定义异步协议，需要根据它的真实响应样本增加任务 ID、状态和结果 URL 映射。

## 剧本多参考图排查记录（2026-08-18）

本次实际操作已完成“剧本 -> 资产 -> 分镜”链路。洛水在分镜阶段提示“分镜失败”，并未发出图片请求；界面给出的直接原因是剧本缺少 `1-1`、`第一场`、`场景1` 等可识别场景标记，整章无法拆分为独立分镜。因此这一次不能把失败归因于图片模型或多参考图接口。

同时检查桥接代码发现第二个确定性问题：Gemini `:generateContent` 图片请求的旧转换逻辑只提取 `contents[].parts[].text`，会丢弃 `inlineData`、`fileData` 参考图。现已修复为保留所有参考图到上游 `images` 数组，并增加只记录数量的诊断日志 `IMAGE Gemini references: N`，不记录图片内容和密钥。后续用格式正确的剧本重试时，应同时检查该数量是否等于洛水界面选择的参考图数量。
